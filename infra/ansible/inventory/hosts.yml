# Ansible inventory for Karb
# IPs from OpenTofu output

all:
  children:
    bot:
      hosts:
        karb-bot:
          ansible_host: 54.196.113.202
          ansible_user: ubuntu
    proxy:
      hosts:
        karb-proxy:
          ansible_host: 3.96.193.114
          ansible_user: ubuntu

  vars:
    ansible_python_interpreter: /usr/bin/python3
    # Proxy configuration
    proxy_port: 1080
    proxy_user: karb
    proxy_pass: "{{ lookup('env', 'KARB_PROXY_PASS') | default('changeme', true) }}"
